{
  "name": "llm-council-backend",
  "version": "1.0.0",
  "description": "LLM Council - TypeScript backend",
  "type": "module",
  "main": "dist/main.js",
  "scripts": {
    "dev": "bun --watch src/main.ts",
    "build": "bun run build:tsc",
    "build:tsc": "bun x tsc",
    "start": "bun run dist/main.js",
    "test": "bun test",
    "test:run": "bun test --run",
    "test:ui": "bun x vitest --ui",
    "test:coverage": "bun test --coverage",
    "typecheck": "bun x tsc --noEmit",
    "lint": "bun x ultracite check",
    "lint:fix": "bun x ultracite fix",
    "format": "bun x ultracite fix",
    "prepare": "bun x husky"
  },
  "dependencies": {
    "@effect/platform": "^0.73.0",
    "@effect/platform-node": "^0.73.0",
    "dotenv": "^16.6.1",
    "effect": "^3.8.0",
    "hono": "^4.10.6",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.7",
    "@types/bun": "^1.1.15",
    "@types/node": "^20.19.25",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3",
    "ultracite": "^6.3.6",
    "vitest": "^1.6.1"
  },
  "engines": {
    "bun": ">=1.0.0"
  }
}
