# Mock Mode Configuration
# Use this for UI testing without real LLM calls

# Enable mock mode - uses realistic fake LLM responses
MOCK_MODE=true

# Optional: These are ignored in mock mode, but you can set them
OPENROUTER_API_KEY=mock-key-not-used
OPENROUTER_API_URL=https://openrouter.ai/api/v1/chat/completions

# Server configuration
PORT=8001

# Council model configuration (also ignored in mock mode)
COUNCIL_MODELS=openai/gpt-5.1,google/gemini-3-pro-preview,anthropic/claude-sonnet-4.5,x-ai/grok-4
CHAIRMAN_MODEL=google/gemini-3-pro-preview
